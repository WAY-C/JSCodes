今日完成：
bilibili客户端面试
VueCLI的安装与使用
render函数，修改默认配置，props，mixin，ref，插件，scoped的使用方法
奇安信笔试题一套
小红书笔试题一套


今日复习知识点：

css加载不会阻塞DOM树的解析
css加载会阻塞DOM树的渲染
css加载会阻塞后面js语句的执行
1.iframe会阻塞主页面的onload事件；
2.搜索引擎检索程序无法解读这种页面，不利于SEO；
3.会影响页面的并行加载。
并行加载：同一时间对同一域名下的加载数量是有限制的：
MDN解析：当初始HTML文档已完全加载和解析时，将触发DOMContentLoaded事件，而不需要等待样式表，图像和子框架页面加载（事件可以用来检测HTML页面是否完全加载完毕(fully-loaded)）。

cookie中设置了httponly后js脚本将无法获取cookie信息，这样可以防范xss攻击
